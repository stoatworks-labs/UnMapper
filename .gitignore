
# Local release artifacts (scripts/release-local.sh)
# Show files written while testing. A real show lives wherever the operator
# saved it; anything at the repo root is scratch.
**/*.rs.bk
*.log
.DS_Store
/*.unmapper.json
/dist-release/
/target
dist-release/
