[package]
name = "unmapper-render"
description = "wgpu rendering for UnMapper: the emulation canvas and the 3D previz view"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
unmapper-core = { path = "../unmapper-core" }
wgpu = { workspace = true }
bytemuck = { workspace = true }
glam = { workspace = true }
gltf = { version = "1.4", default-features = false, features = ["import", "utils"] }
anyhow = { workspace = true }
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "bmp", "tiff", "webp"] }
tracing = { workspace = true }
pollster = { workspace = true }
